[package]
name = "iptrie"
version = "0.5.3"
authors = ["Xophe <christophe.dousson@orange.com>"]
edition = "2021"
license = "MIT"
description = "IPv4/v6 addresses and prefixes lookup structures and algorithms"
repository = "https://github.com/Orange-OpenSource/rust-iptrie"
keywords = ["ip","trie"]
categories = ["data-structures", "algorithms"]

[dependencies]
ipnet = { version = "2" }
lux =  { git = "https://tess7:hmMiKbdt139v9vWAtifZ@gitlab.tech.orange/tess7/framework.git", optional = true }

[dev-dependencies]
libc = { version = "0.2" }
termcolor = { version = "1.1" }

[profile.release]
lto = "thin"
panic = "abort"

[features]
# Activate this feature to generate trie structures in a graphviz file
# (if graphviz is installed, a PDF file could be directly generated by using dot)
graphviz = [ "lux" ]
